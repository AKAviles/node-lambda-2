org: akaviles024
app: node-lambda-2
service: node-lambda-2
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  apiGateway:
    restApiId: ${cf:shared-gateway-dev.apiGatewayRestApiId}
    restApiRootResourceId: !Ref rApiGatewayAdmins
functions:
  admins:
    handler: index.handler
    events:
      - http:
          path: users
          method: get

resources:
  - Resources:
      rApiGatewayAdmins:
        Type: AWS::ApiGateway::Resource
        Properties:
          ParentId: ${cf:shared-gateway-dev.apiGatewayRestApiRootResourceId}
          PathPart: admins
          RestApiId: ${cf:shared-gateway-dev.apiGatewayRestApiId}
  - Outputs:
      oApiGatewayAdminsId:
        Value:
          Ref: rApiGatewayAdmins
        Export:
          Name: admins-apigateway
      oApiGatewayAdminUsersId:
        Value:
          Ref: ${self:functions.admins.events.http.path}
        Export:
          Name: users-empty-path
